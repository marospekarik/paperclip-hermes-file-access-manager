{
  "name": "@ordillect/file-access-manager",
  "version": "0.2.0",
  "description": "Paperclip plugin that manages the Hermes write sandbox (HERMES_WRITE_SAFE_ROOT) per agent",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "NODE_ENV=production bun run build.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": "2026.609.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.3.0",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js"
  }
}
